<?xml version="1.0" encoding="utf-8"?>
<!-- Android shell. Portrait-locked fullscreen, tilt + tap control. No icon
     resource is shipped (the game is asset-free), so the launcher shows the
     platform default icon. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="YIKERS"
        android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">

        <activity
            android:name="com.yikers.android.AndroidLauncher"
            android:exported="true"
            android:label="YIKERS"
            android:screenOrientation="portrait"
            android:configChanges="keyboard|keyboardHidden|navigation|orientation|screenSize|screenLayout">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
